---
title: 와이어프레임
description: 로그인/미팅 생성/상세/확정/참석확인 화면 설계, 플로우, 어노테이션 통합 정리
---

import { Aside, Card, CardGrid } from "@astrojs/starlight/components";
import WireframeFull from "../../../components/WireframeFull.astro";

로그인부터 일정 확정 이후 참석 확인 단계까지, 핵심 화면의 와이어프레임 의도를 본문에서 바로 읽을 수 있도록 정리한 문서입니다. 시각 자료는 보조로만 두고, 화면 구성·동작·정책을 글 중심으로 통합했습니다.

<Aside type="note" title="현재 버전과 연결">
  - 인증 API 상세는 [인증 플로우](/planning/auth/)에서 이어집니다.
  - 시간 선택 동작은 [시간 선택 테이블](/planning/time-select/)에서 이어집니다.
  - 응답 스키마는 [Meeting API (Swagger)](/api/meeting/)에서 검증합니다.
</Aside>

---

## 1. 로그인 (`/login`)

### 1.1 사용자 플로우

1. `/login` 진입
2. 소셜 로그인 버튼 클릭 → `GET /api/auth/{provider}` (302 → OAuth)
3. 사용자 인증 완료 → `GET /api/auth/{provider}/callback`
4. 성공: `Set-Cookie: session` + `/`로 이동
5. 실패: `/login?error={code}`로 복귀

### 1.2 화면 구성

<CardGrid>
  <Card title="Screen 01 — Login Default" icon="rocket">
    제목, 부제, 소셜 버튼 3종(네이버/카카오/구글), 약관 안내. 비로그인 상태에서 진입한 기본 화면.
  </Card>
  <Card title="Screen 02 — Login Error" icon="warning">
    `?error=` 쿼리 파라미터를 읽어 상단 에러 배너를 노출. 버튼 영역은 동일하게 유지.
  </Card>
  <Card title="Screen 03 — Home (Authenticated)" icon="approve-check">
    상단 NavBar(로고/아바타/로그아웃), 중앙 CTA "새 미팅 만들기". 로그인 직후 진입 지점.
  </Card>
  <Card title="Screen 04 — Mobile (375px)" icon="laptop">
    동일한 단일 컬럼 구조. 버튼 패딩을 키워 44px+ 터치 타겟 확보.
  </Card>
</CardGrid>

<Aside type="tip" title="시각 미리보기">
  실제 픽셀 단위 와이어프레임 화면을 본문에서 바로 확인할 수 있습니다. 토글 버튼/번호 마커는 원본 인터랙션 그대로 동작합니다.
</Aside>

<WireframeFull src="/wireframes/01.auth-wireframe-login.html" title="로그인 와이어프레임" />

### 1.3 어노테이션

<CardGrid>
  <Card title="① 진입 시 세션 확인" icon="approve-check">
    `GET /api/auth/me`가 200이면 `/`로 리다이렉트. → Edge E1
  </Card>
  <Card title="② Guard 리다이렉트" icon="setting">
    비로그인 사용자가 보호 페이지 접근 시 `/login`으로 이동. 미들웨어 처리.
  </Card>
  <Card title="③ 소셜 로그인 버튼" icon="rocket">
    클릭 시 `GET /api/auth/{provider}` → 302 → OAuth 페이지 이동.
  </Card>
  <Card title="④ 에러 배너" icon="warning">
    URL `?error=` 파라미터를 읽어 상단 배너 노출. 코드별 메시지로 매핑.
  </Card>
  <Card title="⑤ 에러 코드 매핑" icon="information">
    `auth_failed` / `token_exchange` / `user_info` / `server_error`. i18n 가능.
  </Card>
  <Card title="⑥ NavBar 사용자" icon="user">
    `GET /api/auth/me` 200 → 정보 표시, 401 → 비로그인 상태 처리.
  </Card>
  <Card title="⑦ 아바타 폴백" icon="seti:image">
    `profileImage`가 있으면 `<img>`, 없으면 이름 첫 글자를 원형 배경에 노출.
  </Card>
  <Card title="⑧ 로그아웃" icon="close">
    `POST /api/auth/logout` → 세션 쿠키 삭제 → `/login` 이동.
  </Card>
  <Card title="⑨ 터치 타겟 44px+" icon="laptop">
    모바일 버튼 패딩으로 최소 44×44px 보장 (WCAG 2.5.5).
  </Card>
  <Card title="⑩ 반응형 대응" icon="puzzle">
    `max-w-sm` + `px-4` 기반. 별도 미디어 쿼리 불필요.
  </Card>
</CardGrid>

### 1.4 API 엔드포인트

| Method | Path | 설명 | 응답 |
|--------|------|------|------|
| GET | `/api/auth/{provider}` | 소셜 로그인 시작 | 302 → OAuth |
| GET | `/api/auth/{provider}/callback` | OAuth 콜백 | 302 → `/` 또는 `/login?error=` |
| POST | `/api/auth/logout` | 로그아웃 | 200 `{ ok: true }` |
| GET | `/api/auth/me` | 현재 사용자 조회 | 200 user / 401 |

### 1.5 엣지 케이스 & 정책

- **E1** 이미 로그인된 상태로 `/login` 접근 → `/`로 리다이렉트
- **E2** JWT 만료 → `GET /me` 401 → 세션 쿠키 삭제 → `/login`
- **E3** OAuth 제공자 거부(사용자 취소) → `/login?error=auth_failed`
- **E4** 지원하지 않는 provider → `/login?error=auth_failed`
- **E5** 동일 이메일 다른 제공자 → 별도 계정으로 처리(추후 연동 검토)

### 1.6 구현 상태

- [x] 로그인 페이지 UI (소셜 버튼 3종)
- [x] OAuth 라우트 (`{provider}`, `callback`, `logout`, `me`)
- [x] JWT 발급/검증 (Hono JWT HS256)
- [x] 에러 메시지 표시
- [x] 로그인 후 홈 리다이렉트 + NavBar
- [x] 이미 로그인 시 `/login → /` 리다이렉트
- [x] JWT 만료 시 자동 로그아웃 처리 (`apiFetch` + `exp` 클레임 검사)

---

## 2. 미팅 생성 (`/meeting/new`)

### 2.1 Feature Brief

| 항목 | 값 | 메모 |
|------|-----|------|
| Route | `/meeting/new` | 인증 필수, 미로그인 → `/login` |
| Layout | 싱글 페이지 | 한 페이지 스크롤로 모든 입력 완료 |
| 참고 | When2meet | 날짜 → 시간 범위 → 공유 |
| 시간 범위 | 00:00 ~ 24:00 | 30분 단위, 48 슬롯/일 |
| 공유 방식 | 링크 + 초대 코드 | 생성 후 모달에서 복사 |
| 재사용 컴포넌트 | `Calendar`, `TimeGrid` | date-picker, time-picker 위젯 |

### 2.2 사용자 플로우

1. 홈 `/`에서 "새 미팅 만들기" 클릭
2. `/meeting/new`에서 제목·설명·장소·후보 날짜·시간 범위 입력
3. `POST /api/meetings` 호출
4. 성공 → 공유 모달(링크 + 초대 코드) 노출
5. "미팅 페이지로 이동" → `/meeting/{id}` / "홈으로 돌아가기" → `/`
6. 실패 → 입력값 보존하고 에러 토스트 표시

### 2.3 화면 구성

<CardGrid>
  <Card title="Screen 01 — Default" icon="document">
    제목/설명/장소 입력 + Calendar + 시간 범위 + TimeGrid 미리보기 + 제출 버튼.
  </Card>
  <Card title="Screen 02 — Success Modal" icon="approve-check">
    공유 링크(`/m/{shortId}`)와 초대 코드(`ABC-1234`) 복사. 백드롭 클릭으로는 닫히지 않음.
  </Card>
  <Card title="Screen 03 — Mobile (375px)" icon="laptop">
    싱글 컬럼, 큰 버튼, TimeGrid는 가로 스크롤 또는 숨김 처리 가능.
  </Card>
  <Card title="Screen 04 — Validation/Error" icon="warning">
    필수 항목 미입력 시 인라인 에러, 서버 오류 시 상단 배너.
  </Card>
</CardGrid>

<Aside type="tip" title="시각 미리보기">
  Calendar/TimeGrid 미리보기와 성공 모달까지 포함된 실제 와이어프레임을 본문에서 그대로 확인할 수 있습니다.
</Aside>

<WireframeFull src="/wireframes/02.meeting-new-wireframe.html" title="미팅 생성 와이어프레임" />

### 2.4 폼 필드 구성

| 필드 | 필수 | 제약 | 비고 |
|------|------|------|------|
| `title` | 필수 | 1~50자 | 제목 |
| `description` | 선택 | 최대 200자 | 본문 설명 |
| `location` | 선택 | 최대 100자 | 자유 텍스트 |
| `dates` | 필수 | ISO 날짜 1~14개 | `["2026-04-20", ...]` |
| `startTime` | 필수 | `HH:mm` | 기본 `00:00` |
| `endTime` | 필수 | `HH:mm`, `> startTime` | 기본 `24:00` |

### 2.5 어노테이션

<CardGrid>
  <Card title="① 인증 가드" icon="approve-check">
    `useAuth()`로 인증 확인. 비로그인 시 `/login`으로 이동, 로딩 중에는 스켈레톤 표시.
  </Card>
  <Card title="② 폼 검증" icon="setting">
    클라이언트 + 서버 양측 검증. 첫 에러 필드로 `scrollIntoView()`.
  </Card>
  <Card title="③ Calendar 위젯" icon="seti:calendar">
    `minDate=오늘`, `maxDate=오늘+30일`. 드래그 및 Shift+클릭으로 범위 선택.
  </Card>
  <Card title="④ 시간 범위" icon="seti:clock">
    `<select>` 드롭다운, 30분 간격(00:00~23:30). `endTime > startTime` 검증.
  </Card>
  <Card title="⑤ TimeGrid 미리보기" icon="open-book">
    날짜·시간 입력 시 자동 표시. 주최자 시점은 읽기 전용 미리보기.
  </Card>
  <Card title="⑥ 제출 버튼" icon="right-arrow">
    제목 + 날짜 1개 이상 입력 시 활성화. 제출 중에는 스피너로 비활성.
  </Card>
  <Card title="⑦ 성공 모달" icon="approve-check">
    `POST /api/meetings` 201 응답 시 표시. 백드롭 클릭으로 닫히지 않음(ESC만).
  </Card>
  <Card title="⑧ 공유 자원" icon="external">
    `/m/{shortId}` 링크 + 영문/숫자 8자리 초대 코드(`ABC-1234`).
  </Card>
  <Card title="⑨ 후속 액션" icon="rocket">
    "미팅 페이지로 이동" → `/meeting/{id}` 또는 "홈으로 돌아가기" → `/`.
  </Card>
  <Card title="⑩ 모바일 레이아웃" icon="laptop">
    단일 컬럼, 큰 버튼(44px+). TimeGrid는 가로 스크롤 또는 숨김 가능.
  </Card>
  <Card title="⑪ 터치 드래그" icon="puzzle">
    `touch-action: none` + `elementFromPoint()` 패턴으로 모바일 범위 선택 지원.
  </Card>
  <Card title="⑫ 클라이언트 검증" icon="warning">
    제목/날짜 누락, 글자수 초과 시 인라인 에러. 첫 에러로 자동 스크롤.
  </Card>
  <Card title="⑬ 서버 에러 처리" icon="error">
    폼 하단 배너 + 입력값 보존. 네트워크/서버 코드별 메시지 매핑.
  </Card>
</CardGrid>

### 2.6 데이터 모델 요약

`CreateMeetingRequest` (클라이언트 → 서버)

| Field | Type | Required | 설명 |
|-------|------|----------|------|
| `title` | string | 필수 | 미팅 제목(1~50자) |
| `description` | string | 선택 | 설명(최대 200자) |
| `location` | string | 선택 | 장소(최대 100자) |
| `dates` | string[] | 필수 | ISO 날짜 배열 |
| `startTime` | string | 필수 | `HH:mm` |
| `endTime` | string | 필수 | `HH:mm`, `> startTime` |

자세한 응답 스키마는 [Meeting API (Swagger)](/api/meeting/) 참고.

### 2.7 엣지 케이스

- 필수 항목 누락: 인라인 에러 + 첫 필드로 스크롤
- 30일 초과 미래 날짜: Calendar에서 비활성
- 시간 범위 역전(`end <= start`): 선택 직후 인라인 에러
- 네트워크 오류: "인터넷 연결을 확인해주세요" 배너
- 서버 5xx: 입력값 보존 + 재시도 가능 토스트

### 2.8 구현 상태

- [x] `/meeting/new` 라우트 + 인증 가드
- [x] 제목/설명/장소 입력 폼 + 글자수 카운터
- [x] Calendar 통합 (드래그 + Shift+클릭 범위 선택)
- [x] 시간 범위 `<select>` 드롭다운 (30분 간격)
- [x] TimeGrid 미리보기 (읽기 전용)
- [x] `POST /api/meetings` 연동 + 성공 모달(공유 링크/초대 코드)
- [x] 클라이언트 검증 (제목/날짜/글자수)
- [x] 모바일 레이아웃 (단일 컬럼, 44px+ 터치 타겟)
- [x] 터치 드래그 날짜 선택 (`elementFromPoint` 안정화, passive:false touchmove)
- [x] 서버 에러 코드별 메시지 매핑 (`{ code, message }` 응답 파싱)
- [ ] 초대 코드 재발급/만료 정책

---

## 3. 미팅 상세 (`/meeting/{id}`)

### 3.1 핵심 목적

- 응답 현황을 슬롯 단위로 집계해 주최자/참여자가 한눈에 파악
- 주최자가 확정 단계로 넘어가기 전, 미응답자/혼잡 슬롯을 확인

### 3.2 화면 요약

<CardGrid>
  <Card title="Screen 01 — Summary + Grid" icon="open-book">
    상단 미팅 메타, 중단 TimeGrid 집계, 하단 참여자 요약 리스트로 구성됩니다.
  </Card>
  <Card title="Screen 02 — Participant Focus" icon="user">
    참여자 본인 선택 슬롯 강조, 수정 액션, 공유 링크 복사 동선을 제공합니다.
  </Card>
  <Card title="Screen 03 — Host Ops" icon="setting">
    미응답자 필터, 마감 알림, finalize 이동 CTA를 포함합니다.
  </Card>
</CardGrid>

<WireframeFull src="/wireframes/03.meeting-detail-wireframe.html" title="미팅 상세 와이어프레임" />

### 3.3 연결 API

| Method | Path | 설명 | 응답 |
|--------|------|------|------|
| GET | `/api/meetings/{id}` | 미팅 상세 + 요약 조회 | 200 / 404 |
| GET | `/api/meetings/{id}/votes` | 슬롯별 투표 결과 | 200 / 404 |
| PUT | `/api/meetings/{id}/votes` | 내 시간 선택 저장 | 200 / 400 |

---

## 4. 일정 확정 (`/meeting/{id}/finalize`)

### 4.1 핵심 목적

- 후보 슬롯을 비교한 뒤 단일 최종 시간을 확정
- 확정 공지와 알림 옵션을 함께 설정해 후속 운영을 단순화

### 4.2 화면 요약

<CardGrid>
  <Card title="Screen 01 — Host Final Decision" icon="approve-check">
    추천 슬롯 랭킹, 공지 메시지 편집, 운영 옵션 토글, 최종 확정 CTA로 구성됩니다.
  </Card>
  <Card title="Screen 02 — Participant Result" icon="document">
    최종 일정 카드, 주최자 공지, 캘린더 추가/링크 복사 액션을 제공합니다.
  </Card>
</CardGrid>

<WireframeFull src="/wireframes/04.meeting-finalize-wireframe.html" title="미팅 확정 와이어프레임" />

### 4.3 연결 API

| Method | Path | 설명 | 응답 |
|--------|------|------|------|
| POST | `/api/meetings/{id}/finalize` | 최종 시간 확정 + 잠금 | 200 / 400 / 409 |
| GET | `/api/meetings/{id}/final` | 확정 결과 조회 | 200 / 404 |
| POST | `/api/meetings/{id}/notify` | 확정 알림 재발송 | 202 / 400 |

---

## 5. 리마인드/참석확인 (`/meeting/{id}/attendance`)

### 5.1 핵심 목적

- 행사 전 리마인드 발송 및 참석 의사(ack) 수집
- 미응답자를 빠르게 추적해 당일 노쇼 리스크 최소화

### 5.2 화면 요약

<CardGrid>
  <Card title="Screen 01 — Host Reminder Ops" icon="rocket">
    메시지 작성, 즉시/예약/재발송 정책 선택, 참석 상태 요약 보드를 제공합니다.
  </Card>
  <Card title="Screen 02 — Participant Ack Sheet" icon="user">
    가능/지각/불참 상태 선택, 메모 입력, 응답 제출/캘린더 액션을 제공합니다.
  </Card>
</CardGrid>

<WireframeFull src="/wireframes/05.meeting-reminder-attendance-wireframe.html" title="리마인드/참석확인 와이어프레임" />

### 5.3 연결 API

| Method | Path | 설명 | 응답 |
|--------|------|------|------|
| POST | `/api/meetings/{id}/reminders` | 리마인드 생성/큐 등록 | 202 / 400 / 409 |
| GET | `/api/meetings/{id}/attendance/summary` | 참석 응답 집계 조회 | 200 / 404 |
| POST | `/api/meetings/{id}/attendance/ack` | 참여자 참석 응답 제출 | 200 / 400 / 410 |

---

## 6. 회고/다음 단계 (`/meeting/{id}/recap`)

### 6.1 핵심 목적

- 모임 종료 후 운영 인사이트를 공유하고 아카이브 품질을 높입니다.
- 회고 발행 시 다음 모임 초안을 자동 생성해 재모임 전환을 유도합니다.

### 6.2 화면 요약

<CardGrid>
  <Card title="Screen 01 — Host Recap Publisher" icon="document">
    회고 요약, 하이라이트, 다음 모임 clone 옵션을 함께 입력합니다.
  </Card>
  <Card title="Screen 02 — Participant Reaction" icon="user">
    회고 확인 후 별점/코멘트 반응과 다음 모임 후보 진입을 제공합니다.
  </Card>
</CardGrid>

<WireframeFull src="/wireframes/06.meeting-recap-next-step-wireframe.html" title="회고/다음 단계 와이어프레임" />

### 6.3 연결 API

| Method | Path | 설명 | 응답 |
|--------|------|------|------|
| POST | `/api/meetings/{id}/recap` | 회고 작성/발행 | 201 / 400 / 409 |
| GET | `/api/meetings/{id}/recap` | 회고 조회 | 200 / 404 |
| POST | `/api/meetings/{id}/recap/reactions` | 참여자 반응 제출 | 200 / 400 / 410 |
| POST | `/api/meetings/{id}/clone-draft` | 다음 모임 초안 생성 | 201 / 409 |

---

## 7. 모바일 RSVP 단축 플로우 (`/m/{shortId}/rsvp`)

### 7.1 핵심 목적

- 참여자가 모바일 링크 진입 후 10초 내 참석 의사를 남기게 합니다.
- 호스트는 실시간 집계 보드로 미응답자 대응을 빠르게 수행합니다.

### 7.2 화면 요약

<CardGrid>
  <Card title="Screen 01 — Participant Quick RSVP" icon="user">
    3버튼 상태 선택(참석/지각/불참), 선택 메모, 스티키 제출 버튼을 제공합니다.
  </Card>
  <Card title="Screen 02 — Host Live Snapshot" icon="setting">
    실시간 상태 집계와 미응답자 재촉 액션을 한 화면에서 처리합니다.
  </Card>
</CardGrid>

<WireframeFull src="/wireframes/07.meeting-mobile-rsvp-quick-flow-wireframe.html" title="모바일 RSVP 단축 플로우 와이어프레임" />

### 7.3 연결 API

| Method | Path | 설명 | 응답 |
|--------|------|------|------|
| POST | `/api/meetings/{id}/rsvp/quick` | 모바일 RSVP 즉시 제출 | 200 / 400 / 410 |
| POST | `/api/meetings/{id}/rsvp/quick/undo` | 짧은 시간 내 제출 취소 | 200 / 410 |
| GET | `/api/meetings/{id}/rsvp/quick/summary` | 호스트 실시간 집계 | 200 / 404 |
| POST | `/api/meetings/{id}/rsvp/quick/remind` | 미응답자 재촉 알림 발송 | 202 / 400 |

---

## 8. 원본 HTML 새 창으로 열기

위 임베드된 화면은 원본 HTML과 동일합니다. 전체 화면으로 보고 싶다면 아래 링크에서 새 창으로 열 수 있습니다.

- [로그인 와이어프레임 원본 (새 창)](/wireframes/01.auth-wireframe-login.html)
- [미팅 생성 와이어프레임 원본 (새 창)](/wireframes/02.meeting-new-wireframe.html)
- [미팅 상세 와이어프레임 원본 (새 창)](/wireframes/03.meeting-detail-wireframe.html)
- [미팅 확정 와이어프레임 원본 (새 창)](/wireframes/04.meeting-finalize-wireframe.html)
- [리마인드/참석확인 와이어프레임 원본 (새 창)](/wireframes/05.meeting-reminder-attendance-wireframe.html)
- [회고/다음 단계 와이어프레임 원본 (새 창)](/wireframes/06.meeting-recap-next-step-wireframe.html)
- [모바일 RSVP 단축 플로우 와이어프레임 원본 (새 창)](/wireframes/07.meeting-mobile-rsvp-quick-flow-wireframe.html)
